<div id="messages-container">
{% for label, messages in app.flashes %}
    {% for message in messages %}
        <div class="message__{{ label }}">
            {{ message }}
        </div>
    {% endfor %}
{% endfor %}
</div>